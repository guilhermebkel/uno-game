import { makeStyles } from "@material-ui/core"

const useStyles = makeStyles(theme => ({
	headerContainer: {
		backgroundColor: "#131C29",
		width: "100%",
		height: theme.spacing() * 8,
		borderBottom: "1px solid #22303E"
	}
}))

export default useStyles
